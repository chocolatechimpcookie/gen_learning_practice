Test Plans

1) Make sure the main webpage can be loaded.
2) Make sure the application can detect invalid urls.
3) Make sure the application can create short urls.

To see this in action, run the automated tests.